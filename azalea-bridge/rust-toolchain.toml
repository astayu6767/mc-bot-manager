[toolchain]
# Use latest nightly - azalea main branch tracks latest nightly
# Previously pinned to 2024-11-15 which still failed with 0.16.0
# Now using git main which should work with latest nightly
channel = "nightly"
components = ["rustfmt", "clippy"]
